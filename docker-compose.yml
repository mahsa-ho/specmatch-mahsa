services:
  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      DATA_DIR: /data
    volumes:
      - specmatch-data:/data

volumes:
  specmatch-data:
